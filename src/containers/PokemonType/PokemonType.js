import React, { Component } from 'react';
import styles from './PokemonType.scss';

export default class PokemonType extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return (
      <div className={styles.PokemonType}>
        {/* Your code here */}
      </div>
    );
  }
}
